diff --git a/trellis2/modules/image_feature_extractor.py b/trellis2/modules/image_feature_extractor.py
index c3cb515..0370410 100644
--- a/trellis2/modules/image_feature_extractor.py
+++ b/trellis2/modules/image_feature_extractor.py
@@ -83,7 +83,10 @@ class DinoV3FeatureExtractor:
         hidden_states = self.model.embeddings(image, bool_masked_pos=None)
         position_embeddings = self.model.rope_embeddings(image)
 
-        for i, layer_module in enumerate(self.model.layer):
+        # Transformers 5 moves the encoder under model.model. Keep extracting
+        # pre-final-norm features, exactly as the 4.56.2 TRELLIS path does.
+        encoder = self.model if hasattr(self.model, 'layer') else self.model.model
+        for layer_module in encoder.layer:
             hidden_states = layer_module(
                 hidden_states,
                 position_embeddings=position_embeddings,
diff --git a/trellis2/pipelines/rembg/BiRefNet.py b/trellis2/pipelines/rembg/BiRefNet.py
index c71a992..ae7204c 100644
--- a/trellis2/pipelines/rembg/BiRefNet.py
+++ b/trellis2/pipelines/rembg/BiRefNet.py
@@ -8,7 +8,9 @@ from PIL import Image
 class BiRefNet:
     def __init__(self, model_name: str = "ZhengPeng7/BiRefNet"):
         self.model = AutoModelForImageSegmentation.from_pretrained(
-            model_name, trust_remote_code=True
+            # Transformers 5 otherwise infers FP16 from this checkpoint while
+            # preprocessing produces FP32. Preserve the previous FP32 path.
+            model_name, trust_remote_code=True, torch_dtype=torch.float32
         )
         self.model.eval()
         self.transform_image = transforms.Compose(
@@ -39,4 +41,4 @@ class BiRefNet:
         mask = pred_pil.resize(image_size)
         image.putalpha(mask)
         return image
-    
\ No newline at end of file
+    
